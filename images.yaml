images:
- name: cert-manager-cainjector
  registry: quay.io/jetstack
  version: ${CERT_MGR_VER}
- name: cert-manager-controller
  registry: quay.io/jetstack
  version: ${CERT_MGR_VER}
- name: cert-manager-webhook
  registry: quay.io/jetstack
  version: ${CERT_MGR_VER}
- name: cass-operator
  registry: docker.io/k8ssandra
  version: ${CASS_OPER_VER}
- name: k8ssandra-operator
  registry: docker.io/k8ssandra
  version: ${K8SSANDRA_VER}
- name: busybox
  registry: docker.io/library
  version: ${BUSYBOX_VER}
- name: stargate-4_0
  registry: docker.io/stargateio
  version: ${STARGATE_VER}
- name: cassandra-reaper
  registry: docker.io/thelastpickle
  version: ${REAPER_VER}
- name: medusa
  registry: docker.io/k8ssandra
  version: ${MEDUSA_VER}
- name: cass-management-api
  registry: docker.io/k8ssandra
  version: ${CASS_VER}
- name: system-logger
  registry: docker.io/k8ssandra
  version: ${CASS_OPER_VER}
- name: cass-config-builder
  registry: docker.io/datastax
  version: ${CASS_CONFIG_BUILDER_VER}
